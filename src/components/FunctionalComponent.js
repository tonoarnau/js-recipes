const functionalPiece = () => {
    return (`
        Functional or stateless component:</li>  
        'this' is already bind
        There is no state, refs or lifecycle hooks
        Better readability and performance
        `)
}

export default functionalPiece;